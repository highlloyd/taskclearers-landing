# Local development only - use Terraform for production deployments
services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      # Local bind mount for development (ensures correct permissions)
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - NODE_ENV=development
    restart: unless-stopped
