# Database
DATABASE_PATH=./data/taskclearers.db

# Authentication (JWT_SECRET is REQUIRED in production - use a strong 32+ character random string)
# Generate one with: openssl rand -base64 32
JWT_SECRET=your-32-character-secret-key-here
ADMIN_EMAIL_DOMAIN=taskclearers.com

# Admin notifications - receives new application alerts
NOTIFICATION_EMAIL=admin@taskclearers.com

# Microsoft Graph API (Office 365 Email) - for ALL email sending
# Set up Azure AD app registration with these application permissions:
#   - Mail.Send (required for sending emails from any configured mailbox)
#   - Mail.Read (optional, for syncing replies from applicants)
# See: https://docs.microsoft.com/en-us/graph/auth-v2-service
AZURE_CLIENT_ID=
AZURE_TENANT_ID=
AZURE_CLIENT_SECRET=

# Email Identities - configure which mailboxes to send from
# Each identity can be used for different purposes (admin, sales, hiring)
# The Azure AD app must have Mail.Send permission for all these mailboxes

# Admin email - used for magic links, system notifications
EMAIL_ADMIN=admin@taskclearers.com
EMAIL_ADMIN_NAME=TaskClearers Admin

# Sales email - used for sales lead communication
EMAIL_SALES=sales@taskclearers.com
EMAIL_SALES_NAME=TaskClearers Sales

# Hiring/HR email - used for applicant communication
EMAIL_HIRING=careers@taskclearers.com
EMAIL_HIRING_NAME=TaskClearers Careers

# Legacy fallback (optional) - used if no identities are configured
# O365_SHARED_MAILBOX=careers@taskclearers.com

# App
NEXT_PUBLIC_BASE_URL=http://localhost:3000
